#include <stdio.h>
    int main()
    {
        int x=0;
        if (x=1) //Except 0 all numbers(1,2,3..) are True
        {
            printf("hi");
        }
        else
        {
            printf("how are u");
        }
    }